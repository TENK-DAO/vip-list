{
  "name": "vip-list",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release",
    "test": "cargo test",
    "pretest": "yarn build"
  }
}
